{
  "name": "cops-and-robbers",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Prototipo 3D voxel con crediti virtuali, Vite e Three.js.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:standalone": "node scripts/buildStandalone.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "prettier": "3.6.2",
    "vite": "7.1.5"
  }
}
